name: Deploy changes

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
    - name: update
      uses: wei/curl@v1
      with:
        args: -X POST https://l3p3.de/svr/site-update.txt?site=y4r3
